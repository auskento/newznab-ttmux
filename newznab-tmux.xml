<?xml version="1.0" encoding="utf-8"?>
<Container version="2">
  <!-- Core Container Information -->
  <Name>newznab-tmux</Name>
  <Repository>nntmux/newznab-tmux:latest</Repository>
  <Registry>https://hub.docker.com/r/nntmux/newznab-tmux</Registry>
  <TemplateURL>https://raw.githubusercontent.com/NNTmux/newznab-tmux-docker/master/newznab-tmux.xml</TemplateURL>

  <!-- Description -->
  <Overview>All-in-one Usenet indexer with automatic scanning, full-text search, and API access - all services included in one container.</Overview>
  <Description>
Newznab-tmux is a high-performance, self-contained Usenet indexer built on the Laravel framework. This all-in-one container includes everything needed: PHP application, Nginx web server, MariaDB database, Redis cache, Meilisearch indexing, and Supervisor job management.

All services run inside a single container with persistent storage, making deployment simple and efficient.

Key Features:
- Complete Usenet header scanning
- Full-text search via Meilisearch
- RESTful API for external integrations
- Redis-backed caching and job queuing
- Background job processing with Supervisor
- MariaDB database backend
- Multi-protocol media detection
- Self-contained - no external dependencies

Recommended Requirements:
- 16GB+ RAM
- 4+ CPU cores
- 100GB+ storage for indexing
- Network access to Usenet providers
  </Description>

  <!-- Branding and Links -->
  <Category>Usenet</Category>
  <WebUI>http://[IP]:[PORT:8080]/ or http://172.20.0.10/ (if using static IP)</WebUI>
  <TemplateVersion>3.0</TemplateVersion>
  <Icon>https://raw.githubusercontent.com/NNTmux/newznab-tmux/master/public/images/logo.png</Icon>
  <Support>https://github.com/NNTmux/newznab-tmux/discussions</Support>
  <Project>https://github.com/NNTmux/newznab-tmux</Project>
  <License>MIT</License>
  <Donate>https://github.com/sponsors/NNTmux</Donate>

  <!-- Container Configuration -->
  <Network>bridge</Network>
  <Privileged>false</Privileged>
  <Shell>bash</Shell>

  <!-- Networking -->
  <Networking>
    <Mode>bridge</Mode>
    <Publish>
      <Port>
        <HostPort>8080</HostPort>
        <ContainerPort>80</ContainerPort>
        <Protocol>tcp</Protocol>
      </Port>
    </Publish>
  </Networking>

  <!-- Storage Volumes - All services store data in separate mounted paths -->
  <Data>
    <Volume>
      <HostDir>/mnt/user/appdata/newznab/</HostDir>
      <ContainerDir>/var/www/newznab/storage</ContainerDir>
      <Mode>rw</Mode>
    </Volume>
    <Volume>
      <HostDir>/mnt/user/appdata/newznab/config/</HostDir>
      <ContainerDir>/var/www/newznab/config</ContainerDir>
      <Mode>rw</Mode>
    </Volume>
    <Volume>
      <HostDir>/mnt/user/appdata/newznab/nzbs/</HostDir>
      <ContainerDir>/var/www/newznab/storage/nzbs</ContainerDir>
      <Mode>rw</Mode>
    </Volume>
    <Volume>
      <HostDir>/mnt/user/appdata/newznab/mysql/</HostDir>
      <ContainerDir>/data/mysql</ContainerDir>
      <Mode>rw</Mode>
    </Volume>
    <Volume>
      <HostDir>/mnt/user/appdata/newznab/redis/</HostDir>
      <ContainerDir>/data/redis</ContainerDir>
      <Mode>rw</Mode>
    </Volume>
    <Volume>
      <HostDir>/mnt/user/appdata/newznab/meilisearch/</HostDir>
      <ContainerDir>/data/meilisearch</ContainerDir>
      <Mode>rw</Mode>
    </Volume>
  </Data>

  <!-- User Configuration -->
  <Config Name="Web Port" Target="8080" Default="8080" Mode="rw" Type="Port" Display="always" Required="true" Description="Port for web interface (optional, can use static IP instead)">8080</Config>

  <Config Name="Container Network Subnet" Target="CONTAINER_SUBNET" Default="172.20.0.0/16" Mode="rw" Type="Variable" Display="always" Required="false" Description="Docker network subnet (e.g., 172.20.0.0/16 or 192.168.9.0/24). Match your Unraid bridge network">172.20.0.0/16</Config>

  <Config Name="Container Static IP" Target="CONTAINER_IP" Default="172.20.0.10" Mode="rw" Type="Variable" Display="always" Required="false" Description="Static IP for container (e.g., 172.20.0.10 or 192.168.9.225). Must be within the subnet above">172.20.0.10</Config>

  <Config Name="Appdata Path" Target="/mnt/user/appdata/newznab/" Default="/mnt/user/appdata/newznab/" Mode="rw" Type="Path" Display="always" Required="true" Description="Path for application data, database, cache, and all services">/mnt/user/appdata/newznab/</Config>

  <Config Name="App URL" Target="APP_URL" Default="http://localhost:8080" Mode="rw" Type="Variable" Display="advanced" Required="false" Description="Full URL of your Newznab instance (optional)">http://localhost:8080</Config>

  <Config Name="PHP Memory Limit" Target="PHP_MEMORY_LIMIT" Default="512M" Mode="rw" Type="Variable" Display="advanced" Required="false" Description="PHP memory limit (increase for large indexes)">512M</Config>

  <Config Name="PHP Max Execution Time" Target="PHP_MAX_EXECUTION_TIME" Default="300" Mode="rw" Type="Variable" Display="advanced" Required="false" Description="PHP maximum execution time in seconds">300</Config>

  <Config Name="Meilisearch API Key" Target="MEILISEARCH_KEY" Default="changeme" Mode="rw" Type="Variable" Display="advanced" Required="false" Mask="true" Description="Meilisearch API key (auto-generated on first run if not set)">changeme</Config>

  <!-- Environment Variables -->
  <Environment>
    <Variable>
      <Name>APP_NAME</Name>
      <Value>Newznab-tmux</Value>
    </Variable>
    <Variable>
      <Name>APP_ENV</Name>
      <Value>production</Value>
    </Variable>
    <Variable>
      <Name>APP_DEBUG</Name>
      <Value>false</Value>
    </Variable>
    <Variable>
      <Name>TZ</Name>
      <Value>UTC</Value>
    </Variable>
  </Environment>

  <!-- Metadata -->
  <Version>2.0</Version>
  <Date>2025-01-26</Date>
  <GitHub>https://github.com/NNTmux/newznab-tmux</GitHub>
  <Changes>
v3.0 - Auto-credentials and configurable static IP
- Runtime-configurable static IP address
- Auto-generate admin user and service passwords on first run
- Display all credentials in container logs
- Secure password generation (16+ char alphanumeric)
- Credentials stored in .env.generated for reference
- Compatible with Unraid static IP configuration

v2.0 - All-in-one self-contained container
- Single container with all services
- Nginx, PHP-FPM, MariaDB, Redis, Meilisearch
- Supervisor process management
- Simplified deployment and configuration
- Reduced complexity and resource overhead

v1.0 - Initial Unraid template release
- Multi-container orchestration
- Separate services for modularity
  </Changes>
</Container>
